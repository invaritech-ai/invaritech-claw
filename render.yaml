services:
  - type: web
    name: openclaw
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: ICLAW_GATEWAY_PORT
        value: "8080"
      - key: ICLAW_STATE_DIR
        value: /data/.openclaw
      - key: ICLAW_WORKSPACE_DIR
        value: /data/workspace
      - key: ICLAW_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
